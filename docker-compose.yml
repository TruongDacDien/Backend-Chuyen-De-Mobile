version: "3.9"
services:
  backend:
    build: .
    container_name: backend
    ports:
      - "3000:3000"
    environment:
      - PORT
      - MONGO_URI
      - CLOUDINARY_CLOUD_NAME
      - CLOUDINARY_API_KEY
      - CLOUDINARY_API_SECRET
      - JWT_SECRET
    restart: always
    networks:
      - photel-net

  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
    restart: always
    networks:
      - photel-net

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    command: tunnel --config /home/wsladmin/.cloudflared/config.yml run
    volumes:
      - ./cloudflared:/home/wsladmin/.cloudflared
    depends_on:
      - nginx
    restart: always
    networks:
      - photel-net

  networks:
    photel-net:
      driver: bridge
