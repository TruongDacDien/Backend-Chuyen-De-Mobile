services:
  cloudflared:
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN_STAGING}
    environment:
      - CLOUDFLARE_TUNNEL_TOKEN_STAGING=${CLOUDFLARE_TUNNEL_TOKEN_STAGING}
    volumes:
      - ./cloudflared/config-staging.yml:/etc/cloudflared/config-staging.yml
